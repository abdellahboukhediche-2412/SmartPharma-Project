<Window x:Class="SmartPharma.Views.StockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion du stock"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestion du stock"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">

            <TextBox x:Name="txtRecherche"
                     Width="250"
                     Height="30"
                     Margin="0,0,10,0"/>

            <Button Content="Rechercher"
                    Width="100"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnRechercher_Click"/>

            <Button Content="Tout afficher"
                    Width="100"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnToutAfficher_Click"/>

            <Button Content="Stock faible"
                    Width="100"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnStockFaible_Click"/>

            <Button Content="Modifier quantité"
                    Width="130"
                    Height="30"
                    Click="BtnModifierQuantite_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgStock"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="0,0,0,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"
                                    Binding="{Binding Id}"
                                    Width="50"/>

                <DataGridTextColumn Header="Nom"
                                    Binding="{Binding Nom}"
                                    Width="200"/>

                <DataGridTextColumn Header="Prix"
                                    Binding="{Binding Prix}"
                                    Width="100"/>

                <DataGridTextColumn Header="Quantité"
                                    Binding="{Binding QuantiteStock}"
                                    Width="100"/>

                <DataGridTextColumn Header="Date expiration"
                                    Binding="{Binding DateExpiration}"
                                    Width="180"/>

                <DataGridTextColumn Header="État du stock"
                                    Binding="{Binding EtatStock}"
                                    Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="← Retour"
                    Width="120"
                    Height="35"
                    Click="BtnRetour_Click"/>
        </StackPanel>

    </Grid>
</Window>